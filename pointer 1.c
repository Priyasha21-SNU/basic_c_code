//pointer are use to store address of variables
//pointer are use to create run type memory;
//wastge of memory stop kore pointer thats why we use pointer
//in run time opointer will ask you size
//ask again and again for their choice
//link-list
//oly store adress
//alawys starts with * sign
      //pointer
#include<stdio.h>
int main()
{
    int a,b;
    a=5;
    b=10;

    printf("\naddress of a=%x",&a);
    printf("\naddress of b=%x",&b);
    printf("\nvalue of a=%d",a,b);
    return 0;
}
